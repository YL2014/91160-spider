const { Controller } = require('salak')

class BaseController extends Controller {
  // success (data = null, msg = '', code = 0) {
  //   return {
  //     code, msg, data
  //   }
  // }
}

module.exports = BaseController
